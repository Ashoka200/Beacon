# Copy this file to `.env` and fill in your real values.
# `.env` is git-ignored; this template is safe to commit.

# Anthropic API key — powers Claude Opus 4.8 reasoning in agents/base.py.
# Get one at https://platform.claude.com . Without it, the fleet runs in
# deterministic stub mode (no model calls).
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
